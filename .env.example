DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false

TELEGRAM_BOT_TOKEN=<TOKEN>

# AI workforce generation uses the claude CLI (claude_local adapter) — no API key needed.
# Ensure `claude` is in PATH and logged in via subscription auth.
